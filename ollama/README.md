# Подготовка модели
Веса не входят в репозиторий. До пользовательских запросов выполните `ollama pull qwen2.5vl:7b`, затем `ollama create document-ocr -f /config/Modelfile`. Выберите доступную локально vision-модель вместо базовой при необходимости. API никогда не скачивает модель.
